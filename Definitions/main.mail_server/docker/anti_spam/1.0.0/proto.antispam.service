[Unit]
Description=Rspamd running on Docker
After=docker.service
Requires=docker.service

[Service]
Restart=always
ExecStart={{DOCKER.HOME}}/Definitions/Rspamd/start.sh

[Install]
WantedBy=multi-user.target